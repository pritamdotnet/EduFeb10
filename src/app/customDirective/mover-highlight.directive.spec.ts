import { MoverHighlightDirective } from './mover-highlight.directive';

describe('MoverHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MoverHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
